import Vuex from 'vuex';
import Vue from 'vue';

Vue.use(Vuex)

const state  = {}
const actions = {}
const getters = {};
const mutations = {}
const modules = {

}

const store = new Vuex.Store({
    state,
    actions,
    getters,
    mutations,
    modules
})

export default store;